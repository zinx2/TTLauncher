<UserControl x:Class="TTLauncher.Editor.Views.SeriesItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TTLauncher.Editor.Views"
             mc:Ignorable="d" >
    <Grid Width="80" Height="120">
        <Grid.RowDefinitions>
            <RowDefinition Height="4*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Image x:Name="img_Thumbnail" Source="{Binding ThumbnailPath}"/>
        <TextBlock x:Name="tb_Name" Grid.Row="1" TextWrapping="Wrap" Text="{Binding Name}" TextAlignment="Center" FontWeight="Bold" Foreground="White"/>

    </Grid>
</UserControl>
